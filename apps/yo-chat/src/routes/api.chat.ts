/**
 * Chat API Route
 *
 * Uses the durable streaming handler which provides:
 * - Session reconnection from last LSN
 * - Multi-client fan-out
 * - Full session replay
 *
 * Protocol:
 * - Request query/path: sessionId + offset (or /sessions/{sessionId}?offset=...)
 * - Response: NDJSON with {lsn, event} per line
 * - Response headers: X-Session-Id
 */
import { createFileRoute } from '@tanstack/react-router'
import { createChatHandler, type InitializerContext } from '@sweatpants/framework/server'
import {
  createSharedStorage,
  getSharedStores,
  setupDurableStreams as setupDurableStreamsWithConfig,
  createRedisTokenBufferStore,
  createInMemoryRegistryStore,
  type RetentionPolicy,
} from '@sweatpants/framework/chat/durable-streams'
import {
  resolvePersona,
  setupLogger,
} from '@sweatpants/framework/chat'
import {
  createPiAiModelProviderDriver,
  installPiAiModelProvider,
  resolveRuntimeModel,
  type AssistantMessage as ModelAssistantMessage,
  type Message as ModelMessage,
} from '@sweatpants/model-provider-pi-ai'
import {
  ToolRegistryContext,
  PersonaResolverContext,
  MaxIterationsContext,
  PluginRegistryContext,
  McpToolRegistryContext,
  PluginSessionRegistryContext,
  PluginSessionManagerContext,
} from '@sweatpants/framework/chat'
import {
  createPluginRegistryFrom,
  type ToolSessionSamplingProvider,
  type ToolSessionRegistry,
} from '@sweatpants/framework/chat/mcp-tools'
import {
  createInMemoryToolSessionStore,
  createToolSessionRegistry,
  ToolSessionSamplingProviderContext,
} from '@sweatpants/tool-runtime-local'
import {
  createAgentCoreRemoteToolRuntimeClient,
  createAgentCoreToolSessionRegistry,
  createAwsSdkAgentCoreInvoker,
  createInMemoryAgentCoreToolSessionEventStore,
  createInMemoryAgentCoreToolSessionHandleStore,
  createRedisAgentCoreToolSessionEventStore,
  createRedisAgentCoreToolSessionHandleStore,
  type AgentCoreToolRuntimeProfile,
  type AgentCoreToolSessionStores,
  type RedisLikeClient,
} from '@sweatpants/tool-runtime-agentcore'
import { createPluginSessionManager, type PluginSessionManager } from '@sweatpants/framework/server'
import type { McpToolRegistry } from '@sweatpants/framework/server'
import type {
  ExtendedMessage,
  Message,
  SamplingToolCall,
  SamplingToolDefinition,
} from '@sweatpants/framework/chat'
import type {
  RawSampleResultBase,
  RawSampleResultWithParsed,
  RawSampleResultWithToolCalls,
} from '@sweatpants/framework/chat/mcp-tools'
import { extendedMessageToProviderMessage } from '@sweatpants/framework/chat/mcp-tools'
import type { Operation } from 'effection'
import { run, call } from 'effection'
import { env } from '@/env'
import { getSharedRedisClient } from '@/lib/shared-redis'

// Worker URL for tool execution - vite-plugin-node-worker handles bundling
// In dev: transpiles TS and rewrites imports to file:// URLs
// In build: emits as separate chunk with proper relative path
import toolWorkerUrl from '../__generated__/tool-worker.gen.ts?modulePath'

// Auto-discovered tools from src/tools/ (generated by @sweatpants/framework)
import { toolList } from '../__generated__/tool-registry.gen'
import {
  agentCoreToolNames,
  normalizeAgentCoreToolName,
  normalizeAgentCoreToolNames,
  unsupportedAgentCoreToolNames,
} from '../__generated__/agentcore-tool-registry.gen'

// MCP plugin tools (manually imported until auto-discovery is added)
import { bookFlightTool, bookFlightPlugin } from '../tools/book-flight'
import { tictactoeTool, tictactoePlugin } from '../tools/tictactoe'
import { playTttTool, playTttPlugin } from '../tools/play-ttt'

// Combine all tools
const allTools = [...toolList]

// =============================================================================
// SHARED STORAGE (module-level, persists across requests)
// =============================================================================

/**
 * Shared storage for durable streams.
 *
 * This is created once at module load time and persists across all HTTP requests.
 * This enables:
 * - Session reconnection (client can disconnect and reconnect to same session)
 * - Multi-client fan-out (multiple clients reading same session)
 * - Proper cleanup (sessions cleaned up when all clients disconnect AND stream completes)
 *
 * Storage strategy:
 * - REDIS_URL set → Redis-backed TokenBufferStore (durable across restarts)
 * - No REDIS_URL → In-memory shared storage (single-process only)
 */
const sharedStorage = createSharedStorage<string>()
const retainForeverPolicy: RetentionPolicy = { mode: 'retain_forever' }

// Initializer hook that uses Redis if available, otherwise in-memory shared storage
const setupDurableStreams = function* (): Operation<void> {
  const client = yield* call(() => getSharedRedisClient())

  if (client) {
    // Redis-backed TokenBufferStore with in-memory registry store
    const bufferStore = createRedisTokenBufferStore<string>(client)
    const registryStore = createInMemoryRegistryStore()
    yield* setupDurableStreamsWithConfig({
      bufferStore,
      registryStore,
      retentionPolicy: retainForeverPolicy,
    })
  } else {
    // In-memory shared storage
    const { bufferStore, registryStore } = getSharedStores(sharedStorage)
    yield* setupDurableStreamsWithConfig({
      bufferStore,
      registryStore,
      retentionPolicy: retainForeverPolicy,
    })
  }
}

const setupProvider = function* (ctx: InitializerContext): Operation<void> {
  const body = ctx.body as { provider?: string; model?: string }
  const providerName = body.provider || env.CHAT_PROVIDER
  yield* installPiAiModelProvider({ model: resolveRuntimeModel(providerName, body.model) })
}

const setupTools = function* (_ctx: InitializerContext): Operation<void> {
  yield* ToolRegistryContext.set(allTools)
}

const setupPersonaResolver = function* (_ctx: InitializerContext): Operation<void> {
  yield* PersonaResolverContext.set(resolvePersona)
}

const setupMaxIterations = function* (_ctx: InitializerContext): Operation<void> {
  yield* MaxIterationsContext.set(10)
}

async function requestWithDefaultSystemPrompt(request: Request): Promise<Request> {
  if (request.method.toUpperCase() !== 'POST') {
    return request
  }

  const contentType = request.headers.get('content-type') ?? ''
  if (!contentType.includes('application/json')) {
    return request
  }

  let body: unknown
  try {
    body = await request.clone().json()
  } catch {
    return request
  }

  if (!body || typeof body !== 'object' || Array.isArray(body)) {
    return request
  }

  const record = body as Record<string, unknown>
  if (typeof record['systemPrompt'] === 'string' && record['systemPrompt'].trim().length > 0) {
    return request
  }

  const messages = record['messages']
  if (
    Array.isArray(messages)
    && messages.some((message) => {
      return !!message && typeof message === 'object' && (message as { role?: unknown }).role === 'system'
    })
  ) {
    return request
  }

  const headers = new Headers(request.headers)
  headers.delete('content-length')

  return new Request(request.url, {
    method: request.method,
    headers,
    body: JSON.stringify({ ...record, systemPrompt: env.CHAT_SYSTEM_PROMPT }),
    signal: request.signal,
  })
}

// =============================================================================
// PLUGIN SETUP (MCP plugin tools with React UI elicitation)
// =============================================================================

/**
 * Create a simple MCP tool registry from an array of tools.
 */
function createMcpToolRegistry(mcpTools: { name: string }[]): McpToolRegistry {
  const map = new Map<string, unknown>()
  for (const tool of mcpTools) {
    if (env.MCP_TOOL_RUNTIME === 'agentcore') {
      const canonicalName = normalizeAgentCoreToolName(tool.name) ?? tool.name
      const canonicalTool = canonicalName === tool.name
        ? tool
        : { ...(tool as object), name: canonicalName }
      map.set(tool.name, canonicalTool)
      map.set(canonicalName, canonicalTool)
      continue
    }

    map.set(tool.name, tool)
  }
  return {
    get: (name: string) => map.get(name),
    has: (name: string) => map.has(name),
    names: () => Array.from(map.keys()),
  }
}

// MCP plugin tools (with elicitation support)
const mcpPluginTools = [bookFlightTool, tictactoeTool, playTttTool]

// Plugin registry (handlers for elicitation)
// Note: Type assertion needed due to TypeScript variance with generic handlers.
// In AgentCore mode, the model/runtime may use canonical MCP names such as
// `book_flight`, while the app-facing plugin names are generated aliases such
// as `book-flight_book_flight`. Register both names against the same handlers.
const pluginClients = [bookFlightPlugin.client as any, tictactoePlugin.client as any, playTttPlugin.client as any]
const pluginRegistry = createPluginRegistryFrom(
  env.MCP_TOOL_RUNTIME === 'agentcore'
    ? pluginClients.flatMap((plugin) => {
        const canonicalName = normalizeAgentCoreToolName(plugin.toolName)
        return canonicalName && canonicalName !== plugin.toolName
          ? [plugin, { ...plugin, toolName: canonicalName }]
          : [plugin]
      })
    : pluginClients,
)

function rawConfiguredAgentCoreToolNames(): string[] {
  const override = env.AGENTCORE_TOOL_NAMES
    ?.split(',')
    .map((name) => name.trim())
    .filter((name) => name.length > 0)

  return override && override.length > 0 ? override : agentCoreToolNames
}

function configuredAgentCoreToolNames(): string[] {
  const configured = rawConfiguredAgentCoreToolNames()
  const unsupported = unsupportedAgentCoreToolNames(configured)
  if (unsupported.length > 0) {
    throw new Error(
      `Unsupported AGENTCORE_TOOL_NAMES value(s): ${unsupported.join(', ')}. `
      + `Supported AgentCore tool names are: ${agentCoreToolNames.join(', ')}. `
      + 'Canonical intrinsic MCP names are preferred; supported generated aliases are accepted and normalized.'
    )
  }

  return normalizeAgentCoreToolNames(configured)
}

function activeMcpPluginTools(): Array<(typeof mcpPluginTools)[number]> {
  if (env.MCP_TOOL_RUNTIME !== 'agentcore') return mcpPluginTools
  const configured = new Set(configuredAgentCoreToolNames())
  return mcpPluginTools.filter((tool) => {
    const canonicalName = normalizeAgentCoreToolName(tool.name) ?? tool.name
    return configured.has(canonicalName)
  })
}

function activeAgentCoreToolNames(): string[] {
  return configuredAgentCoreToolNames()
}

const activePluginTools = activeMcpPluginTools()
console.info('[yo-chat] active MCP plugin tools', {
  runtime: env.MCP_TOOL_RUNTIME,
  configuredAgentCoreToolNames: env.MCP_TOOL_RUNTIME === 'agentcore' ? configuredAgentCoreToolNames() : rawConfiguredAgentCoreToolNames(),
  availablePluginToolNames: mcpPluginTools.map((tool) => tool.name),
  activePluginToolNames: activePluginTools.map((tool) => tool.name),
})

// MCP tool registry (for tool lookup)
// In AgentCore mode, expose only tools packaged into the remote runtime image.
const mcpToolRegistry = createMcpToolRegistry(activePluginTools)

/**
 * Sampling options interface (mirrors ToolSessionSamplingOptions)
 */
interface SamplingOptions {
  systemPrompt?: string
  maxTokens?: number
  tools?: SamplingToolDefinition[]
  toolChoice?: 'auto' | 'required' | 'none'
  schema?: Record<string, unknown>
}

function providerMessageToModelMessage(message: Message): ModelMessage {
  if (message.role === 'system') {
    return { role: 'system', content: message.content }
  }

  if (message.role === 'user') {
    return { role: 'user', content: message.content }
  }

  if (message.role === 'tool') {
    return {
      role: 'toolResult',
      toolCallId: message.tool_call_id ?? '',
      toolName: message.replay?.toolName ?? '',
      content: [{ type: 'text', text: message.content }],
      isError: message.content.startsWith('Error:'),
    }
  }

  return {
    role: 'assistant',
    content: [
      ...(message.content ? [{ type: 'text' as const, text: message.content }] : []),
      ...(message.tool_calls ?? []).map((toolCall) => ({
        type: 'toolCall' as const,
        id: toolCall.id,
        name: toolCall.function.name,
        arguments: toolCall.function.arguments,
      })),
    ],
  }
}

function assistantText(message: ModelAssistantMessage): string {
  return message.content
    .filter((block): block is Extract<ModelAssistantMessage['content'][number], { type: 'text' }> => block.type === 'text')
    .map((block) => block.text)
    .join('')
}

function assistantToolCalls(message: ModelAssistantMessage): SamplingToolCall[] {
  return message.content
    .filter((block): block is Extract<ModelAssistantMessage['content'][number], { type: 'toolCall' }> => block.type === 'toolCall')
    .map((block) => ({
      id: block.id,
      name: block.name,
      arguments: block.arguments,
    }))
}

function samplingTools(tools: SamplingToolDefinition[] | undefined) {
  return tools?.map((tool) => ({
    name: tool.name,
    description: tool.description ?? '',
    parameters: typeof tool.inputSchema === 'object' && tool.inputSchema !== null
      ? tool.inputSchema as Record<string, unknown>
      : { type: 'object', properties: {}, required: [] },
  }))
}

/** Real sampling provider for plugin tools, backed by the scoped pi-ai model provider driver. */
function createPluginSamplingProvider(): ToolSessionSamplingProvider {
  const driver = createPiAiModelProviderDriver()
  const model = resolveRuntimeModel(env.CHAT_PROVIDER)

  return {
    *sample(messages: ExtendedMessage[], options: SamplingOptions | undefined) {
      const modelMessages = messages
        .map(extendedMessageToProviderMessage)
        .map(providerMessageToModelMessage)

      const assistant = yield* driver.sample({
        model,
        context: {
          ...(options?.systemPrompt ? { systemPrompt: options.systemPrompt } : {}),
          messages: modelMessages,
          ...(options?.tools ? { tools: samplingTools(options.tools) } : {}),
        },
        options: {
          ...(options?.maxTokens !== undefined ? { maxTokens: options.maxTokens } : {}),
          ...(options?.toolChoice ? { toolChoice: options.toolChoice } : {}),
          ...(options?.schema ? { responseFormat: { type: 'json_schema', name: 'plugin_sample', schema: options.schema } } : {}),
        },
      })

      const text = assistantText(assistant)
      const toolCalls = assistantToolCalls(assistant)
      if (toolCalls.length > 0) {
        return {
          text,
          model: model.id,
          stopReason: 'toolUse',
          toolCalls,
        } satisfies RawSampleResultWithToolCalls
      }

      if (options?.schema) {
        try {
          return {
            text,
            model: model.id,
            stopReason: 'endTurn',
            parsed: JSON.parse(text),
          } satisfies RawSampleResultWithParsed<unknown>
        } catch (error) {
          return {
            text,
            model: model.id,
            stopReason: 'endTurn',
            parsed: null,
            parseError: {
              message: error instanceof Error ? error.message : 'Failed to parse JSON',
              rawText: text,
            },
          } satisfies RawSampleResultWithParsed<unknown>
        }
      }

      return {
        text,
        model: model.id,
        stopReason: assistant.stopReason === 'maxTokens' ? 'maxTokens' : 'endTurn',
      } satisfies RawSampleResultBase
    },
  }
}

/**
 * Shared plugin session registry and manager (module-level, persists across requests).
 * 
 * CRITICAL: Both must be created in a long-lived Effection scope so that tool
 * sessions survive across HTTP request boundaries. This enables multi-step
 * elicitation flows where:
 * 
 * 1. Request 1: Tool starts, emits pickFlight elicitation, session stays alive
 * 2. Request 2: User selects flight, tool resumes from the same session
 * 3. Request 2: Tool emits pickSeat elicitation, session stays alive
 * 4. Request 3: User selects seat, tool completes
 * 
 * The manager wraps the registry and tracks:
 * - Session wrappers (for server-side sampling)
 * - Last processed LSN per session (to avoid replaying old events on reconnect)
 * 
 * This pattern mirrors how yo-mcp creates its registry at server startup.
 */
let sharedPluginSessionRegistry: ToolSessionRegistry | null = null
let sharedPluginSessionManager: PluginSessionManager | null = null

const pluginSamplingProvider = createPluginSamplingProvider()

function* createLocalPluginToolSessionRegistry(): Operation<ToolSessionRegistry> {
  const store = createInMemoryToolSessionStore()
  // Worker URL provided by vite-plugin-node-worker
  // In dev: transpiles TS and rewrites imports to file:// URLs
  // In build: emits as separate chunk with proper relative path
  return yield* createToolSessionRegistry(store, {
    samplingProvider: pluginSamplingProvider,
    worker: {
      workerUrl: toolWorkerUrl,
      isDev: process.env['NODE_ENV'] !== 'production',
    },
  })
}

function* createAgentCoreStores(): Operation<AgentCoreToolSessionStores> {
  const redis = yield* call(() => getSharedRedisClient())
  if (!redis) {
    return {
      handles: createInMemoryAgentCoreToolSessionHandleStore(),
      events: createInMemoryAgentCoreToolSessionEventStore(),
    }
  }

  const redisOptions = {
    keyPrefix: env.AGENTCORE_REDIS_KEY_PREFIX,
    defaultTtlSeconds: Math.max(1, Math.ceil(env.AGENTCORE_TOOL_SESSION_TTL_MS / 1000)),
  }
  const client = redis as unknown as RedisLikeClient
  return {
    handles: createRedisAgentCoreToolSessionHandleStore(client, redisOptions),
    events: createRedisAgentCoreToolSessionEventStore(client, redisOptions),
  }
}

function requireAgentCorePaidInvocationApproval(): void {
  if (process.env['APPROVE_AGENTCORE_PAID_INVOCATION'] !== 'yes') {
    throw new Error(
      'MCP_TOOL_RUNTIME=agentcore with AGENTCORE_RUNTIME_ARN may invoke paid AWS Bedrock AgentCore Runtime services. '
      + 'Set APPROVE_AGENTCORE_PAID_INVOCATION=yes for local/development app-mode AgentCore invocation.'
    )
  }
}

function* createAgentCorePluginToolSessionRegistry(): Operation<ToolSessionRegistry> {
  if (!env.AGENTCORE_RUNTIME_ARN) {
    throw new Error('AGENTCORE_RUNTIME_ARN is required when MCP_TOOL_RUNTIME=agentcore')
  }

  requireAgentCorePaidInvocationApproval()

  const toolNames = activeAgentCoreToolNames()
  if (toolNames.length === 0) {
    throw new Error('generated AgentCore tool registry must contain at least one tool when MCP_TOOL_RUNTIME=agentcore')
  }

  const stores = yield* createAgentCoreStores()
  const runtimeClient = createAgentCoreRemoteToolRuntimeClient(
    createAwsSdkAgentCoreInvoker({ clientConfig: { region: env.AGENTCORE_REGION } })
  )
  const profile: AgentCoreToolRuntimeProfile = {
    name: env.AGENTCORE_RUNTIME_PROFILE_NAME,
    runtimeArn: env.AGENTCORE_RUNTIME_ARN,
    endpointName: env.AGENTCORE_ENDPOINT_NAME,
    region: env.AGENTCORE_REGION,
    toolNames,
    maxSessionTtlMs: env.AGENTCORE_TOOL_SESSION_TTL_MS,
  }

  return createAgentCoreToolSessionRegistry({
    stores,
    runtimeClient,
    profiles: [profile],
    defaultProfile: profile.name,
  })
}

function redactAgentCoreArn(value: string | undefined): string | undefined {
  if (!value) return undefined
  const suffix = value.split('/').pop()?.slice(-8) ?? 'unknown'
  return `[redacted-agentcore-runtime:${suffix}]`
}

function* createConfiguredPluginToolSessionRegistry(): Operation<ToolSessionRegistry> {
  if (env.MCP_TOOL_RUNTIME === 'agentcore') {
    console.info('[yo-chat] MCP tool runtime: agentcore', {
      runtimeArn: redactAgentCoreArn(env.AGENTCORE_RUNTIME_ARN),
      endpointName: env.AGENTCORE_ENDPOINT_NAME,
      region: env.AGENTCORE_REGION,
      toolNames: activeAgentCoreToolNames(),
    })
    return yield* createAgentCorePluginToolSessionRegistry()
  }

  console.info('[yo-chat] MCP tool runtime: local')
  return yield* createLocalPluginToolSessionRegistry()
}

// Create the registry and manager in a long-lived scope at module initialization.
// This runs once when the module is first imported.
const registryPromise = run(function* () {
  const registry = yield* createConfiguredPluginToolSessionRegistry()
  const manager = yield* createPluginSessionManager({ registry, samplingProvider: pluginSamplingProvider })
  sharedPluginSessionRegistry = registry
  sharedPluginSessionManager = manager
  return { registry, manager }
})

const setupPlugins = function* (_ctx: InitializerContext): Operation<void> {
  yield* PluginRegistryContext.set(pluginRegistry)
  yield* McpToolRegistryContext.set(mcpToolRegistry)
  yield* ToolSessionSamplingProviderContext.set(pluginSamplingProvider)

  // Wait for registry and manager to be ready. They are created at module load,
  // but the first HTTP request can race initialization in dev/serverless mode.
  if (!sharedPluginSessionRegistry || !sharedPluginSessionManager) {
    const ready = yield* call(() => registryPromise)
    sharedPluginSessionRegistry = ready.registry
    sharedPluginSessionManager = ready.manager
  }

  yield* PluginSessionRegistryContext.set(sharedPluginSessionRegistry)
  yield* PluginSessionManagerContext.set(sharedPluginSessionManager)
}

// Create the handler with hook-based configuration
// IMPORTANT: setupLogger must be first, setupDurableStreams must run before others
const chatHandler = createChatHandler({
  initializerHooks: [
    setupLogger, // Must be first to enable logging in other hooks
    setupDurableStreams,
    setupProvider,
    setupTools,
    setupPersonaResolver,
    setupMaxIterations,
    setupPlugins, // MCP plugin tools with React UI elicitation
  ],
})

export const Route = createFileRoute('/api/chat')({
  server: {
    handlers: {
      GET: async ({ request }) => {
        return chatHandler(request)
      },
      HEAD: async ({ request }) => {
        return chatHandler(request)
      },
      POST: async ({ request }) => {
        return chatHandler(await requestWithDefaultSystemPrompt(request))
      },
    },
  },
})

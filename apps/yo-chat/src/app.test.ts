// Basic test to ensure the app builds and runs
import { describe, it, expect } from 'vitest'

describe('yo-chat app', () => {
  it('should have basic functionality', () => {
    expect(true).toBe(true)
  })
})